# استخدام اسم الكمبيوتر للاتصال بالسيرفر

## ✅ تم التحديث

تم تحديث ملف `.env` لاستخدام اسم الكمبيوتر بدلاً من IP:

```env
EXPO_PUBLIC_DOMAIN=AZIZ-PC.local:5000
```

---

## 📱 كيفية الاتصال من الهاتف

### الخطوة 1: تأكد من أن الهاتف والكمبيوتر في نفس الشبكة المحلية

### الخطوة 2: في التطبيق المحمول، استخدم:
```
http://AZIZ-PC.local:5000
```

أو يمكنك استخدام الاسم بدون `.local`:
```
http://AZIZ-PC:5000
```

---

## 🔧 إذا لم يعمل `.local`

بعض الشبكات لا تدعم mDNS (`.local`). في هذه الحالة:

### الخيار 1: استخدم اسم الكمبيوتر مباشرة
```env
EXPO_PUBLIC_DOMAIN=AZIZ-PC:5000
```

### الخيار 2: استخدم IP الحالي
```env
EXPO_PUBLIC_DOMAIN=10.141.160.209:5000
```

---

## 🌐 المميزات

- ✅ **لا يتغير** حتى لو تغير IP
- ✅ **سهل التذكر** - اسم بدلاً من أرقام
- ✅ **يعمل في أي شبكة محلية** (إذا كانت تدعم mDNS)

---

## ⚠️ ملاحظات مهمة

1. **يعمل فقط في الشبكة المحلية** - لا يعمل من الإنترنت
2. **يحتاج Firewall مفتوح** - تأكد من فتح منفذ 5000
3. **بعض الشبكات لا تدعم `.local`** - في هذه الحالة استخدم الاسم بدون `.local`

---

## 🧪 اختبار الاتصال

من الهاتف (في نفس الشبكة):

1. افتح المتصفح
2. اذهب إلى: `http://AZIZ-PC.local:5000/admin`
3. إذا ظهرت صفحة تسجيل الدخول، فالاتصال يعمل! ✅

---

## 🔄 إعادة تشغيل التطبيق

بعد التحديث، أعد تشغيل:

```bash
# أوقف السيرفر (Ctrl+C)
# ثم شغّله مرة أخرى
npm run server:dev
```

وفي التطبيق المحمول، أعد تشغيل التطبيق لتحميل الإعدادات الجديدة.
