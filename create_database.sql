-- إنشاء قاعدة بيانات محلية لتطبيق Meter Reader
-- قم بتشغيل هذا الملف باستخدام PostgreSQL

-- إنشاء قاعدة البيانات
CREATE DATABASE meter_reader;

-- الاتصال بقاعدة البيانات (اختياري - سيتم تطبيق الـ schema لاحقاً)
-- \c meter_reader
